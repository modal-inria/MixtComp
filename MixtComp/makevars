#-----------------------------------------------------------------------
# makevars
# author: Serge Iovleff
#-----------------------------------------------------------------------
#
# This file has to be included in all makefile of the projects.
#
#-----------------------------------------------------------------------
#
#   HASRMATH 't' if your system has libRmath
HASRMATH = t

LIB_RMATH_A = /usr/lib/libRmath.a
R_FLAGS = -DMATHLIB_STANDALONE

#-----------------------------------------------------------------------
# make program
# MAKE  = make

#-----------------------------------------------------------------------
# compilers
CC    = gcc
CXX   = g++

#-----------------------------------------------------------------------
# specific C flags
# long long are available
CFLAGS   = -Wno-long-long

#-----------------------------------------------------------------------
# STK 0 based adressing by default
STK_FLAGS = -DSTKBASEARRAYS=0

#-----------------------------------------------------------------------
# flags in Debug mode
STK_FLAGS_DEBUG = $(STK_FLAGS) -DSTK_BOUNDS_CHECK -DSTK_DEBUG -DSTK_VERBOSE -DSTK_MIXTURE_VERY_VERBOSE
CXXFLAGS_DEBUG  = "-ansi -std=c++11 -pedantic -Wall -g -O0 $(STK_FLAGS_DEBUG)"

#-----------------------------------------------------------------------
# flags in Release mode
#STK_FLAGS_RELEASE = $(STK_FLAGS)
CXXFLAGS_RELEASE = "-ansi -std=c++11 -pedantic -Wall -O3 -ftree-vectorize -march=native -funroll-loops $(STK_FLAGS_RELEASE)"
